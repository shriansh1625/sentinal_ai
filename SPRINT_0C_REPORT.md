# SPRINT_0C_REPORT — Browser UI

**Finding owners:** F-003, F-004  
**Decision:** **GO**

## Changes

- Added `lit` dependency
- Lit popup (`sentinel-popup`) + options (`sentinel-options`)
- Manifest `default_popup` + `options_page`
- Brand-first serif header; no React

## ADR-034

Compliant — production UI is Lit custom elements.
